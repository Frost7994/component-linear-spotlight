// components
import Spotlight from "@/components/spotlight";

const Home = () => {
    return (
        <div className="relative flex h-screen w-screen items-center justify-center">
            <Spotlight background="#000000" fromColor="#b4a3ea" toColor="#8b71de" />
        </div>
    );
};

export default Home;
